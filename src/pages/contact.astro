---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead
			title="Contact | TikaTiles"
			description="Contact information for TikaTiles."
		/>
	</head>
	<body>
		<Header />
		<main class="contact-page">
			<section class="contact-card">
				<p class="eyebrow">Contact</p>
				<h1>Contact</h1>
				<h2>Ritika Goel</h2>
				<p>
					Ritika lives in Orange County with her husband and 8-year-old daughter. TikaTiles is
					being built from her personal lab as a playful hardware experiment for kids and families.
					Over the last 10 years, she has worked at Apple and Meta, building her profile as a
					systems embedded engineer.
				</p>
				<div class="contact-links">
					<a href="https://www.linkedin.com/in/ritikagoel23/" target="_blank">LinkedIn</a>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	.contact-page {
		display: grid;
		place-items: center;
		min-height: calc(100vh - 220px);
	}

	.contact-card {
		width: min(760px, 100%);
		padding: clamp(1.5rem, 4vw, 3rem);
		border: 1px solid var(--line);
		border-radius: 8px;
		background: var(--surface-raised);
		box-shadow: var(--box-shadow);
	}

	.contact-card h1 {
		font-size: clamp(4rem, 11vw, 8rem);
		line-height: 0.9;
	}

	.contact-card h2 {
		margin-bottom: 1rem;
		font-size: clamp(1.4rem, 3vw, 2rem);
		color: rgb(var(--gray-dark));
	}

	.eyebrow {
		margin: 0 0 0.75rem;
		color: var(--accent);
		font-size: 0.78rem;
		font-weight: 800;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.contact-card p {
		color: rgb(var(--gray-dark));
		text-indent: 0;
	}

	.contact-links {
		display: flex;
		flex-wrap: wrap;
		gap: 0.8rem;
		margin-top: 1.5rem;
	}

	.contact-links a {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 42px;
		padding: 0 1rem;
		border: 1px solid rgb(var(--black));
		border-radius: 999px;
		background: rgb(var(--black));
		color: var(--page-bg);
		font-weight: 800;
		text-decoration: none;
	}

	.contact-links a:hover {
		color: var(--page-bg);
	}
</style>
